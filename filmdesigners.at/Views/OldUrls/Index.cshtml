@model IEnumerable<filmdesigners.at.Models.OldUrl>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.URL)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MemberID)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.URL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemberID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OldUrlID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OldUrlID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OldUrlID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
